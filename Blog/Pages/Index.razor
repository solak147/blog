@page "/"
@inherits Blog.Base.Base

<h1><b>簡介</b></h1>
<br />

<img src="images/index_1.jpg" alt="gcpdocker_2" class="indexbanner imgfmt">
<br />
<p>
<b>
    我是一個全端工程師，在此會記錄我學習中碰到的各種問題<br />
    包括 網頁、手機、前後端、架站環境部屬...等等<br />
    讓我們一同學習 ~ 一同進步~                     
    <br />
    <span style="float:right">最後更新日期 : 2023-03-11</span>
</b>
</p>

@code {
    public override async Task SetParametersAsync(ParameterView parameters)
    {
        parameters.SetParameterProperties(this);
        
        seo.isDrawCode = false;

        seo.pageTitle = "中二病也要寫程式 - Blog";
        seo.metaTitle = "中二病也要寫程式 - Blog";
        seo.metaDescription = @"中二病也要寫程式 - Blog | 分享前後端程式學習經驗，程式語言教學 Program teaching : C#、Go、Dart、JavaScript、CSS
            流行框架 Framework : Vue + Nuxt、ASP.NET Core MVC、Blazor、Flutter
            環境部屬 Environment deployment : Docker、GCP、GitHub Pages";

        await base.SetParametersAsync(ParameterView.Empty);
    }
}